using marketboard.Application.Interfaces.Repositories;
using marketboard.Domain.Entities.Catalog;

namespace marketboard.Infrastructure.Repositories
{
    public class BrandRepository : IBrandRepository
    {
        private readonly IRepositoryAsync<Brand, int> _repository;

        public BrandRepository(IRepositoryAsync<Brand, int> repository)
        {
            _repository = repository;
        }
    }
}